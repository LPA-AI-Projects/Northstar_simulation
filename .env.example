# Copy to .env for local runs (Railway injects DATABASE_URL automatically).
DATABASE_URL=postgresql://postgres:password@localhost:5432/northstar
PORT=3000
# Set to "disable" only for local Postgres without SSL
PGSSLMODE=disable
